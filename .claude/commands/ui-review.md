Review the UI and UX of the specified component or page in this React + TypeScript mixology app.

If the user specifies a component or file, focus on that. Otherwise, review the current state of the overall UI.

For each area reviewed, provide:
1. **What's working** — keep it brief, only note genuinely good decisions
2. **UX issues** — friction points, confusing flows, missing affordances
3. **Visual/design issues** — layout, spacing, hierarchy, consistency
4. **Specific improvements** — concrete, actionable changes with code snippets where helpful

Context about this app:
- React 19 + TypeScript + Vite frontend
- No component library — currently vanilla CSS with heavy inline styles
- Pages: Home, Recipe List, Recipe Detail, Recipe Form (create/edit), Ingredient List, Category List
- Backend: Django REST API at localhost:8000

Keep feedback prioritized — lead with the highest-impact issues. Be direct and specific, not generic.
